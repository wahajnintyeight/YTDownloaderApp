/**
 * Screen name constants using enum for type safety and DRY principle
 * Centralized source of truth for all screen names used in analytics and navigation
 */
export enum ScreenNames {
  Browse = 'Browse',
  Downloads = 'Downloads',
  Settings = 'Settings',
  VideoViewer = 'VideoViewer',
  Splash = 'Splash',
}
