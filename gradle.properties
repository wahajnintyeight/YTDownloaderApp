# Top-level Gradle properties for EAS CI
# Ensure AndroidX and Jetifier are enabled so dependency resolution works
android.useAndroidX=true
android.enableJetifier=true

# Enable Hermes and new architecture flags globally so subprojects can read them
hermesEnabled=true
newArchEnabled=true
